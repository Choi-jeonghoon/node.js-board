import response from 'express';
import request from 'supertest';
import { describe, expect, test } from '@jest/globals';
// describe('boardAPI', () => {
//   it('GET /board ---> array board ', () => {});

//   it('POST /board/id ---> specific board by ID ', () => {});

//   it('POST /board/id ---> 404 if not found ', () => {});

//   it('PUT /board/id ---> specific board by ID ', () => {});
// });
describe('테스트 단위', () => {
  beforeAll(() => {
    /* 모든 test() 함수 실행 이전에 작동한다. */
  });

  afterAll(() => {
    /* test() 함수가 모두 실행된 이후에 작동한다. */
  });

  test('무슨 테스트인지 여기에 작성한다.', () => {});
});
